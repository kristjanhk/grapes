dependencies {
    compile project(':common')

    compile group: 'io.vertx', name: 'vertx-web', version: "${cfg.ver.vertx}"
}

mainClassName = 'eu.kyngas.grapes.music.Launcher'

shadowJar {
    classifier = 'music'
}